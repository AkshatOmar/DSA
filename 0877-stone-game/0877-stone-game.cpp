class Solution {
public:
    // bool helper(vector<int>&piles, int n, int i, int j) {
    //     left = piles[i] + helper(piles,n,i+1,j);
    //     right = piles[j]+helper(piles,n,i,j+1);


    // }
    bool stoneGame(vector<int>& piles) {
        int n = piles.size();
        //helper(piles, n, 0,n-1);
        return true;
    }
};